@model TrainingWebsite.ViewModels.profileViewModel
<div class="card">
    <div class="card-body profile-card pt-4 d-flex flex-column align-items-center">
        @if (Model.Image != null)
        {
            <img src="data:image/*;base64,@(Convert.ToBase64String(Model.Image))" alt="Profile" class="rounded-circle">
        }
        else
        {
            <img id="profilePicture" alt="Profile" class="rounded-circle" src="">
        }
        <h2>@Model.FullName</h2>
        <h3>@Model.JobName</h3>
        <div class="social-links mt-2">
            <a href="#" class="twitter"><i class="bi bi-twitter"></i></a>
            <a href="#" class="facebook"><i class="bi bi-facebook"></i></a>
            <a href="#" class="instagram"><i class="bi bi-instagram"></i></a>
            <a href="#" class="linkedin"><i class="bi bi-linkedin"></i></a>
        </div>
    </div>
</div>
